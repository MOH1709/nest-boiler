<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome</title>
  <style>
    /* General Styles */
    body {
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-color: #f5f5f5;
      font-family: 'Arial', sans-serif;
    }

    h3 {
      margin: 0;
      color: #333;
      font-size: 2em;
      opacity: 0;
      animation: fadeInUp 1s ease-in-out forwards;
    }

    h1 {
      margin: 0;
      color: #007bff;
      font-size: 3em;
      opacity: 0;
      animation: fadeInUp 1.5s ease-in-out forwards;
      animation-delay: 0.5s;
    }

    /* Keyframes for Animation */
    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }

      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Button for extra visual element */
    .btn {
      display: inline-block;
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #007bff;
      color: white;
      font-size: 1.2em;
      border-radius: 5px;
      text-decoration: none;
      opacity: 0;
      animation: fadeInUp 2s ease-in-out forwards;
      animation-delay: 1s;
    }

    .btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>

<body>
  <div>
    <h3>Welcome, This is the Nest Boilerplate</h3>
    <h1>Initiated by <%= data.name %></h1>
    <a href="https://github.com/MOH1709/nest-boiler" class="btn">Learn More</a>
  </div>
</body>

</html>